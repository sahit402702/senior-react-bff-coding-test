/**
 * UsersPage Component
 * 
 * TODO: Implement this page to display a list of all users
 * 
 * Requirements:
 * 1. Use the useUsers hook to fetch users data
 * 2. Show <Loader /> component while loading
 * 3. Show <ErrorMessage /> component if there's an error
 * 4. Display the list of users once loaded
 * 5. Each user should be a clickable link to /users/:id
 * 
 * Hints:
 * - Import Link from 'react-router-dom' for navigation
 * - Map through the users array to display each user
 * - Show user name and email in each card
 */

export default function UsersPage() {
  // TODO: Implement the component
  return (
    <div className="page">
      <h1>Users</h1>
      <p>TODO: Implement user list</p>
    </div>
  )
}