/**
 * ProductsPage Component
 * 
 * TODO: Implement this page to display a grid of products
 * 
 * Requirements:
 * 1. Use the useProducts hook to fetch products
 * 2. Show <Loader /> component while loading
 * 3. Show <ErrorMessage /> component if there's an error
 * 4. Display the products in a grid once loaded
 * 5. Each product should be a clickable link to /products/:id
 * 6. Display product image, title, and price for each product
 * 
 * Hints:
 * - Import Link from 'react-router-dom' for navigation
 * - Map through the products array to display each product
 * - Use the .products-list class for the grid container
 * - Use the .product-card class for each product
 */

export default function ProductsPage() {
  // TODO: Implement the component
  return (
    <div className="page">
      <h1>Products</h1>
      <p>TODO: Implement product list</p>
    </div>
  )
}