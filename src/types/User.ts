/**
 * User interface based on JSONPlaceholder API response
 * 
 * TODO: Define the User interface with the following properties:
 * - id: number
 * - name: string
 * - email: string
 * - username: string
 * - company: object with name property (string)
 * 
 * Optional properties you may add:
 * - phone: string
 * - website: string
 * - address: object (if needed)
 * 
 * Hint: Focus on the properties you'll display: name, email, company.name
 */

// TODO: Export User interface
