import type { User } from '../types/User'

/**
 * User Service - API functions for fetching users
 * 
 * TODO: Implement fetchUsers function
 * 
 * Requirements:
 * 1. Use native fetch API (no axios)
 * 2. Fetch from: https://jsonplaceholder.typicode.com/users
 * 3. Return Promise<User[]>
 * 4. Handle errors properly (throw with meaningful message)
 * 5. Check response.ok before parsing JSON
 * 
 * Example structure:
 * export async function fetchUsers(): Promise<User[]> {
 *   const response = await fetch(url)
 *   if (!response.ok) throw new Error(...)
 *   return response.json()
 * }
 */

// TODO: Implement fetchUsers function
